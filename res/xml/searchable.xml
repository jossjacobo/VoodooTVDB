<?xml version="1.0" encoding="utf-8"?>
<searchable
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_label_for_seachbar"
    android:hint="@string/search_hint"
    android:searchSettingsDescription="suggests symbols"

    android:searchMode="showSearchLabelAsBadge"
    android:queryAfterZeroResults="true"
    android:searchSuggestAuthority=".utils.SuggestionProvider"
    android:searchSuggestSelection=" ? "
    android:searchSuggestIntentAction="android.intent.action.VIEW"
    android:searchSuggestIntentData="content://someAuthority/somePath" >
</searchable>
